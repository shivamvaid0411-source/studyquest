/* ============================================================
   SUBJECTS ARRAY — populated by the files in js/data/*.js
   Load order in index.html: this file first, then each subject
   file (which calls SUBJECTS.push(...)), in any order after this.
   ============================================================ */
const SUBJECTS = [];
